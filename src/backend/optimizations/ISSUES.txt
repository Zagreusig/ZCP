is_boundary() eats global _start :waaaa:

is_push() / is_pop() don't do anything with the new IR

is_noop() doesn't really do anything anymore
   - add reg, 0 / sub reg, 0 never happens in the new backend generator
   - try_to_xor / same_xor still works :D

is_cond()
   - missing setx conditions