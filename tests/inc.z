#include "tests/loops.z"
#include "tests/an_actual_program.z"
#include "tests/test_header.zh"
#include "tests/loops.z"

fn func_stub(int a, int b) -> int {
   return a * b;
}

fn main() -> int {
   loops();
   program();
   println(test_func(9, 10));
   println(a_thing(1, 2));

   this_errors(9, 10);
}