fn main(): int {
   have arr: int[10];
   for (have i = 0; i < 10; i++) {
      arr[i] = i;
   }
   for (have i = 0; i < 10; i++) {
      println(arr[i]);
   }
   /*
      This is a comment block
   */

   // This is a comment

   return 0;
}